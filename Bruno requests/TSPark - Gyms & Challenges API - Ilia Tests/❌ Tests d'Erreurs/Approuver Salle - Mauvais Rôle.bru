meta {
  name: Approuver Salle - Mauvais Rôle
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/gyms/{{new_gym_id}}/approve
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{client_token}}
}

script:post-response {
  test("Accès refusé", function () {
      expect(res.getStatus()).to.equal(403);
  });
  
  console.log("✅ Contrôle des rôles fonctionnel");
}

settings {
  encodeUrl: true
}

docs {
  Tentative d'approbation avec un rôle client (devrait échouer)
}
