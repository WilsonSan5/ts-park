meta {
  name: Get User Stats
  type: http
  seq: 3
}

get {
  url: {{base_url}}/users/{{clientUserId}}/stats
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{superAdminToken}}
}

settings {
  encodeUrl: true
}

docs {
  Get user statistics (workouts, challenges, badges).
}
