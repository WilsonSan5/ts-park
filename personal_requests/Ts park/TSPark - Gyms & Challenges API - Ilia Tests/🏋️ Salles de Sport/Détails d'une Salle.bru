meta {
  name: Détails d'une Salle
  type: http
  seq: 3
}

get {
  url: {{base_url}}/gyms/{{new_gym_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{gym_owner_token}}
}

script:post-response {
  test("Détails récupérés", function () {
      expect(res.getStatus()).to.equal(200);
  });
  
  test("Données complètes", function () {
      var jsonData = res.getBody();
      expect(jsonData.data).to.have.property('id');
      expect(jsonData.data).to.have.property('name');
      expect(jsonData.data).to.have.property('description');
      expect(jsonData.data).to.have.property('equipment');
  });
  
  console.log("🏋️ Salle:", res.getBody().data.name);
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Obtenir les détails d'une salle spécifique
}
