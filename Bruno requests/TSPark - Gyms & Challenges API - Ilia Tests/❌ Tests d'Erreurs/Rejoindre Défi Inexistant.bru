meta {
  name: Rejoindre Défi Inexistant
  type: http
  seq: 2
}

post {
  url: {{base_url}}/challenges/id-inexistant/join
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{client_token}}
}

script:post-response {
  test("Erreur 404 correcte", function () {
      expect(res.getStatus()).to.equal(404);
  });
  
  console.log("✅ Défi inexistant correctement géré");
}

settings {
  encodeUrl: true
}

docs {
  Tentative de rejoindre un défi qui n'existe pas
}
