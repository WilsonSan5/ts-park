meta {
  name: Créer Salle - Non Autorisé
  type: http
  seq: 1
}

post {
  url: {{base_url}}/gyms
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{gym_owner_token}}
}

body:json {
  {
    "name": "Salle Sans Auth",
    "description": "Ceci devrait échouer"
  }
}

script:post-response {
  test("Rejet correct", function () {
      expect(res.getStatus()).to.equal(401);
  });
  
  console.log("✅ Requête non autorisée correctement rejetée");
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Tentative de création de salle sans authentification
}
