meta {
  name: Données Invalides - Création Défi
  type: http
  seq: 4
}

post {
  url: {{base_url}}/challenges
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{gym_owner_token}}
}

body:json {
  {
    "title": "",
    "description": "Défi sans titre",
    "startDate": "2023-01-01",
    "endDate": "2022-12-31"
  }
}

script:post-response {
  test("Validation des données", function () {
      expect(res.getStatus()).to.equal(400);
  });
  
  console.log("✅ Validation des données fonctionnelle");
}

settings {
  encodeUrl: true
}

docs {
  Créer un défi avec des données invalides
}
