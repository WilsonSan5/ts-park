meta {
  name: Quitter un Défi (Client)
  type: http
  seq: 8
}

post {
  url: {{base_url}}/challenges/{{new_challenge_id}}/leave
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{client_token}}
}

script:post-response {
  test("Départ réussi", function () {
      expect(res.getStatus()).to.equal(204);
  });
  
  console.log("❌ Défi quitté avec succès");
}

settings {
  encodeUrl: true
}

docs {
  Quitter un défi en cours
}
